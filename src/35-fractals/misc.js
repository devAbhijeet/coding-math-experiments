// const pA = {
//   x: (p0.x + p1.x) / 2,
//   y: (p0.y + p1.y) / 2
// };
// const pB = {
//   x: (p1.x + p2.x) / 2,
//   y: (p1.y + p2.y) / 2
// };
// const pC = {
//   x: (p2.x + p0.x) / 2,
//   y: (p2.y + p0.y) / 2
// };
// drawTraingle(p0, pA, pC);
// drawTraingle(pA, p1, pB);
// drawTraingle(pC, pB, p2);
